const Menu = () => {
  return (
    <div className="hidden md:block">
      <button type="button">Sign in</button>
      <button type="button">Sign up</button>
    </div>
  );
};

export default Menu;
